import { THEME_COLORS } from '../theme/colors';

export const COLORS = {
	SONPI16_ORANGE: THEME_COLORS['sonpi16-orange'],
	SONPI16_BLUE: THEME_COLORS['sonpi16-blue'],
	SONPI16_BLACK: THEME_COLORS['sonpi16-black'],
	SONPI16_GOLD: THEME_COLORS['sonpi16-gold']
} as const;

export const FONTS = {
    QUENCY_PIXEL: "QuencyPixel-Regular"
} as const;