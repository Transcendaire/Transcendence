{
  "dependencies": {
    "better-sqlite3": "^12.2.0",
    "fastify": "^5.6.0"
  },
  "name": "fastify",
  "version": "1.0.0",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.4.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "all": "tsc -p tsconfig.json && node dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "db": "rm -rf transcendaire.db && npx ts-node src/database.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
